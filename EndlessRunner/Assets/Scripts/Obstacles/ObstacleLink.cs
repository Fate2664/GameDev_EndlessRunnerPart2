using UnityEngine;

public class ObstacleLink : MonoBehaviour
{
    //This script links the obstacle config to a monobehaviour script so that we can assign a scriptable object onto each obstacle prefab
    public ObstacleConfig obsConfig;
}
